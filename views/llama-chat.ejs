<h2>Chat with LLaMA</h2>
<form id="llamaForm">
  <input type="text" name="prompt" placeholder="Ask anything...">
  <button type="submit">Send</button>
</form>
<div id="llamaResponse"></div>

<script>
  document.getElementById("llamaForm").addEventListener("submit", async (e) => {
    e.preventDefault();
    const prompt = e.target.prompt.value;
    const res = await fetch("/chat/llama", {
      method: "POST",
      body: JSON.stringify({ prompt }),
      headers: { "Content-Type": "application/json" }
    });
    const data = await res.json();
    document.getElementById("llamaResponse").innerText = data.response;
  });
</script>