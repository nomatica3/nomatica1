<h2>Chat with Gemma AI</h2>
<form id="gemmaForm">
  <input type="text" name="prompt" placeholder="Ask anything...">
  <button type="submit">Send</button>
</form>
<div id="gemmaResponse"></div>

<script>
  document.getElementById("gemmaForm").addEventListener("submit", async (e) => {
    e.preventDefault();
    const prompt = e.target.prompt.value;
    const res = await fetch("/chat/gemma", {
      method: "POST",
      body: JSON.stringify({ prompt }),
      headers: { "Content-Type": "application/json" }
    });
    const data = await res.json();
    document.getElementById("gemmaResponse").innerText = data.response;
  });
</script>